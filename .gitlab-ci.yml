compile:
  stage: build
  image: arkark/latexmk:full
  script:
    - echo "Compiling LaTeX with TexLive."
    - rm /root/.latexmkrc
    - latexmk -pdf thesis.tex
  tags:
    - latex
  artifacts:
    paths:
      - ./*.pdf
