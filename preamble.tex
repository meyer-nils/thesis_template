% Language and fonts
\usepackage[ngerman, english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{microtype}
\usepackage{setspace}
\usepackage[all]{nowidow}
\usepackage{palatino}

% Bibliography
\usepackage{csquotes}
\usepackage[backend=biber, style=ieee, citestyle=numeric-comp, sorting=none, date=year, isbn=false, url=false, maxcitenames=2, mincitenames=1]{biblatex}
\addbibresource{literature.bib}

% Enumerations
\usepackage{enumitem}
\setlist[enumerate]{nosep}
\setlist[itemize]{nosep}
\setlist[trivlist]{nosep}
\setlist[description]{leftmargin=\parindent,labelindent=\parindent}

% Page and paragraph geometry 
\usepackage{geometry}
\textheight230mm		
\textwidth160mm
\voffset-5mm
\hoffset-25mm
\evensidemargin25mm
\oddsidemargin25mm
\parindent0mm
\parskip1mm
\fboxrule0.1mm
\fboxsep1mm
\linespread{1.3}

% Math
\usepackage{mathtools}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{bbm}

% Units
\usepackage{siunitx}

% Captions 
\usepackage[format=plain,indention=0cm, labelfont=bf, font=footnotesize]{caption}
\usepackage[font=footnotesize, labelfont=normalfont]{subcaption}

% Graphics
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{float}
\usepackage[absolute]{textpos}
% Uncomment pgfplots if you create plots directly in LaTeX (e.g. with \addplot).
% Most students include figures as image files and do not need it.
% \usepackage{pgfplots}
% \pgfplotsset{compat=1.18}

% Tables
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{color}
\usepackage{makecell}
\setcellgapes{4pt}

% Verbatims — used for the example chapter. If you remove that chapter and do not
% need verbatim code blocks yourself, you can comment these out.
\usepackage{fancyvrb}
\usepackage{verbdef}

% Acronyms
\usepackage[printonlyused]{acronym}

% Months
\newcommand{\clearemptydoublepage}{\newpage{\pagestyle{empty}\cleardoublepage}}
\newcommand{\monthword}[1]{\ifcase#1\or January\or February\or March\or April\or May\or June\or July\or August\or September\or October\or November\or December\fi}


% Headers and footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.pt}
\renewcommand{\footrulewidth}{0.pt}
\setlength\headheight{16pt}
\makeatletter
\renewcommand{\chaptermark}[1]{\markboth{\thechapter\ #1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}

	\fancypagestyle{empty}{
		\fancyhf{}
		\renewcommand{\headrulewidth}{0.pt}
		\renewcommand{\footrulewidth}{0.pt}
	}

	% Title pages
	\fancypagestyle{title}{
	\fancyhead{}
	\fancyfoot{}
    \fancyhead[OL]{
        \setlength{\unitlength}{1mm}
        \begin{picture}(0,0)
            \put(-10,-30){\includegraphics[height=35mm]{images/logos/mrm.png}}
        \end{picture}
      }

    \fancyhead[OR]{
        \setlength{\unitlength}{1mm}
        \begin{picture}(0,0)
            \put(-60,-25){\includegraphics[height=35mm]{images/logos/uni_a.png}}
        \end{picture}
    }

    \begin{tikzpicture}[remember picture, overlay]
        \centering
        \begin{tikzpicture}[remember picture, overlay,shift={(80mm,-115mm)}]
            \draw(87.5mm,-127mm) arc (270:360:5mm) 
              -- (92.5mm,130mm) arc (0:90:5mm) 
              --(-87.5mm,135mm) arc (90:180:5mm)
              --(-92.5mm,-122mm) arc(180:270:5mm) 
              -- (87.5mm,-127mm);
        \end{tikzpicture};
    \end{tikzpicture}
	}

     % Regular pages
	\fancypagestyle{plain}{%
		\renewcommand{\headrulewidth}{0pt}%
		\renewcommand{\footrulewidth}{0.1pt}%
		\fancyhf{}%
		\fancyfoot[C]{\thepage}
	}

	% Main pages
	\fancypagestyle{main}{
		\fancyhead{}
		\fancyfoot{}
		\renewcommand{\headrulewidth}{0.1pt}		
		\renewcommand{\footrulewidth}{0.1pt}
		\fancyhead[L]{\nouppercase \leftmark}
		\fancyhead[C]{}
		\fancyfoot[C]{\thepage}
	}

% Chapter titles
\makeatletter
\def\@makechapterhead#1{%
  \vspace*{50\p@}%
  {\parindent \z@ \raggedright \normalfont
    \ifnum \c@secnumdepth >\m@ne
      \if@mainmatter
        %\Huge\bfseries \@chapapp\space \thechapter
        \Huge\bfseries \thechapter \hspace{0.8cm}%
        %\par\nobreak
        %\vskip 20\p@
      \fi
    \fi
    \interlinepenalty\@M
    \Huge \bfseries #1\par\nobreak
    \vskip 40\p@
  }}
\makeatother
\usepackage{titlesec}
\titlespacing{\section}{0pt}{\parskip}{-\parskip}
\titlespacing{\subsection}{0pt}{\parskip}{-\parskip}
\titlespacing{\subsubsection}{0pt}{\parskip}{-\parskip}


% PDF options
\usepackage[breaklinks=true,citebordercolor={1 1 1},linkbordercolor={1 1 1}, hidelinks]{hyperref}
\hypersetup{bookmarksopen,
            bookmarksopenlevel=1,
            bookmarksnumbered,
            pdfhighlight=/I,
            pdfpagemode=UseOutlines,
            english,
            pdflang=en,
            pdfdisplaydoctitle,
}

% Naming the DOI entry.
\usepackage{doi}
\renewcommand{\doitext}{DOI: }


\usepackage{multirow}         % multi-row cells in tables
\usepackage[normalem]{ulem}   % \uline, \sout etc. (normalem keeps \emph italic)
\usepackage{booktabs}         % professional table rules (\toprule, \midrule, \bottomrule)


\usepackage{threeparttable}   % tables with footnotes

 \usepackage[section]{placeins}

